export enum MODELS {
  PROMPTS = 'PROMPTS',
  USERS = 'USERS',
  TOKENS = 'TOKENS',
  ORGANIZATIONS = 'ORGANIZATIONS',
}

export enum TokenTypes {
  ACCESS = 'access',
  REFRESH = 'refresh',
  RESET_PASSWORD = 'resetPassword',
}
